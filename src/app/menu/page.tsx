import { Header } from "@/components/common/header";

export default function Menu({
  searchParams,
}: {
  searchParams: { search: string };
}) {
  return (
    <>
      <Header />
      <div>menuuuuu</div>
    </>
  );
}
